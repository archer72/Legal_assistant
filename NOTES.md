Guidance & next steps
- Use accelerated model loading (accelerate/bitsandbytes) for large HF models.
- Persist conversation memory in Redis for scale.
- Add authentication, rate-limiting and logging/metrics for production.
- Add CI tests that validate ingestion and a small sample RAG query.
